<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application

        android:allowBackup="true"
        android:name=".FinanceAppApplication"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/frag_img_4"
        android:label="@string/app_name"
        android:roundIcon="@drawable/frag_img_4"
        android:supportsRtl="true"
        android:theme="@style/Theme.FinanceApp"
        tools:targetApi="31">
        <activity
            android:name=".presentation.Presentation"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
<!--<activity android:name=".presentation.FinanceAppActivity"/>-->
<activity android:name=".presentation.MainActivity"/>


    </application>

</manifest>